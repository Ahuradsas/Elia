{
  "name": "edglam-demo",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "node --import=@yucacodes/ts --import=./env.mjs ./src/run.ts",
    "build": "tsc --noEmit && tsup-node ./src/run.ts --format esm --clean --no-splitting --sourcemap",
    "start": "node ./dist/run.js",
    "fmt": "prettier --write .",
    "tsc": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@wabot-dev/framework": "0.4.0-beta.2"
  },
  "devDependencies": {
    "@swc/core": "^1.15.8",
    "@types/ws": "^8.18.1",
    "prettier": "^3.5.3",
    "tsup": "^8.5.0"
  }
}
